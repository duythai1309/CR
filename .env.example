NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Trợ lý ảo. Thiếu khoá thì trợ lý tự tắt, phần còn lại của nền tảng vẫn chạy bình thường.
GEMINI_API_KEY=
# Bỏ trống để dùng mặc định gemini-2.5-flash.
GEMINI_MODEL=

# Token để eval platform gọi POST /api/eval/chat. Trống thì endpoint tự tắt (503).
EVAL_API_TOKEN=
