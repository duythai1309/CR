node_modules/
.next/
out/
build/
.env
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store
.vercel

# Video gốc độ phân giải cao — bản đã nén nằm ở public/video/
media-goc/

# Log thô của bộ kiểm chứng DB cục bộ (tests/db/run.py) — sinh lại được bằng cách chạy lại.
# Giữ results.json vì các báo cáo trong docs/design trích dẫn chúng làm bằng chứng.
tests/db/runs/**
!tests/db/runs/
!tests/db/runs/*/
!tests/db/runs/*/results.json

# Tài liệu gốc của Verra, Gold Standard và CDM — tải về từ trang chính thức của họ.
# Không commit: 41 MB, và đây là tài liệu có bản quyền của bên thứ ba, repo này công khai.
# Xem reference/README.md để biết lấy ở đâu.
reference/methodologies/

# Cấu hình quyền cá nhân cho máy này, không chia sẻ.
.claude/settings.local.json
